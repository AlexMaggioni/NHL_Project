distance_to_goal : true # add a column with the distance to the goal
angle_to_goal : true # add a column with the angle to the goal
is_goal : true # add a column with a boolean indicating if the shot is a goal
empty_net : true # add a column with a boolean indicating if the goal is empty
verbose : True 
input_rinkSide : True # add a column with the mean of the rinkSide column